Please use these rules:

All type declarations go into central type file.

All tests go into central test file, in semantic subdirectories per feature or type (components, lib, hooks, contexts).

Add tests for newly implemented features/changes.

Run lint and tests after implementing changes. 

